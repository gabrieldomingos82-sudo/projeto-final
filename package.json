{
  "name": "ademicon-whatsapp-server",
  "version": "2.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx puppeteer browsers install chrome || echo 'Aviso: download explícito do Chrome falhou, seguindo com o cache padrão do Puppeteer'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.5",
    "whatsapp-web.js": "^1.34.7",
    "qrcode": "^1.5.3"
  }
}
